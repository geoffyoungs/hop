version: 2

project_name: hop

git:
  ignore_tags:
    - nightly

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go run ./cmd/gendocs
    - ./scripts/completions.sh

builds:
  - main: ./cmd/hop
    binary: hop
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README.md
      - docs/man/*
      - completions/*

homebrew_casks:
  - name: hop
    binaries:
      - hop
    manpages:
      - docs/man/*.1
    completions:
      bash: completions/hop.bash
      zsh: completions/hop.zsh
      fish: completions/hop.fish
    repository:
      owner: geoffyoungs
      name: homebrew-tap
    directory: Casks
    skip_upload: auto
    homepage: https://github.com/geoffyoungs/hop
    description: "CLI tool for connecting to named hosts via SSH, Docker, or Kubernetes"

nfpms:
  - package_name: hop
    homepage: https://github.com/geoffyoungs/hop
    maintainer: Geoff Youngs <git@intersect-uk.co.uk>
    description: "CLI tool for connecting to named hosts via SSH, Docker, or Kubernetes"
    license: MIT
    formats:
      - deb
      - rpm
    contents:
      - src: docs/man/hop.1
        dst: /usr/share/man/man1/hop.1
      - src: docs/man/hop-completion.1
        dst: /usr/share/man/man1/hop-completion.1
      - src: completions/hop.bash
        dst: /usr/share/bash-completion/completions/hop
      - src: completions/hop.zsh
        dst: /usr/share/zsh/vendor-completions/_hop
      - src: completions/hop.fish
        dst: /usr/share/fish/vendor_completions.d/hop.fish

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  # Set to auto to skip during local builds
  skip_upload: auto
